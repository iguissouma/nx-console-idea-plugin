{
  "name": "nx-cli",
  "version": "1.0.0",
  "dependencies": {
    "@angular/cli": "^11.2.5",
    "json5": "^2.1.3",
    "jsonc-parser": "^3.0.0"
  },
  "scripts": {
    "build:clean": "npm run clean && npm run build",
    "build": "npm run build:node-modules && npm run build:plugin",
    "clean": "rm -Rf ../../gen/nxCli",
    "build:plugin": "tsc",
    "build:node-modules": "mkdir -p ../../gen/nxCli && cp -Rf ./package.json ../../gen/nxCli && cd ../../gen/nxCli && npm install --only=prod && node-prune && rm -rf node_modules/rxjs/_esm5/ && rm -rf node_modules/rxjs/_esm2015/ && rm -rf node_modules/rxjs/bundles/"
  },
  "devDependencies": {
    "@types/node": "~12.12.5",
    "typescript": "4.0.7",
    "@nrwl/workspace": "^11.5.1"
  }
}
